import { StyleSheet } from 'react-native';

export const mainStyle = StyleSheet.create({
    main: {
        flex: 3,
        flexDirection: 'column',
        alignItems: 'center',
        backgroundColor: '#ccc',
        paddingTop: 30,
    }
});